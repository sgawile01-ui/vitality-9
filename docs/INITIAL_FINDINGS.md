# Inspection before implementation

2026-09-10. Cloned the requested repository into a new local directory; clean main at a8b65c2. Only main existed remotely. Created local branch fix/vitality9-mvp-readiness. No AGENTS.md or hosting configuration found. No environment files read.

The repository has 35 tracked files, no package manifest/lockfile, HTML entry point, stylesheet, Vite configuration, UI primitives, tests or generated Convex modules. TypeScript configs already exist. React/Vite/Convex versions and a package manager were not pinned; npm and Node 22 are restoration choices, not recovered historical facts. Host has Node 22.23.2 and npm 10.9.8. Source uses React, motion, Tailwind-style utility classes, Convex, Hercules OIDC and Stripe. The backend imports absent challengeData; frontend has nine descriptions but no 81 activities.

Coach authorization checks identity and isPro, but history is trusted as model-role content, inputs have no size limits, model is gemini-1.5-flash, no timeout exists, raw provider errors are logged, and the prompt mainly relies on a disclaimer. Payments are reachable without a testing gate. Day/task indices lack validation, progress assumes three tasks/day, last-day completion and reset counts are incorrect. Some pillar claims are unsupported. No original dependency versions can be verified from the source.
